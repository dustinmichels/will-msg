Go project, with GUI by Fyne.

The goal is to parse addresses, issues, and other data from Outlook .msg files.

All issues pertain to trash and recycling pickup.

For debugging, can use `go run ./cmd/msgcat <file.msg>` to see the plain-text body of the .msg file.
